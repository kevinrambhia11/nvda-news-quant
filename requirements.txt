# Core pipeline + dashboard. Version pins matter for Streamlit Community
# Cloud: the committed .joblib models must unpickle in the cloud container,
# so scikit-learn is pinned to the training version.
pandas>=3.0
numpy>=2.0
requests>=2.31
yfinance>=0.2.40
scikit-learn==1.9.0
vaderSentiment>=3.3.2
feedparser>=6.0
beautifulsoup4>=4.12
joblib>=1.3
matplotlib>=3.7
streamlit>=1.57

# BigQuery transport for the competitors series (works locally with the
# service-account key; degrades gracefully to cached data without it)
google-cloud-bigquery>=3.25
db-dtypes>=1.2

# Optional - higher-quality financial sentiment via FinBERT (~2 GB download):
# torch
# transformers
